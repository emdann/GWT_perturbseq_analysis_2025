# Metadata 

- `experiments_config.yaml` - config file to preprocess experiments (input to `src/1_preprocess/preprocess.py`)
- `LRZ002RHSgRNA.csv` - raw sgRNA library metadata (provided by Jun)
- `sgRNA_library_curated.csv` - curated sgRNA library metadata (generated by `src/1_preprocess/prep_sgrna_library_metadata.ipynb`)
